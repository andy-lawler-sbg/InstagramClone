import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  scrollView: {
    maxHeight: 110,
    paddingBottom: 10,
  },
  container: {
    paddingLeft: 12,
    flexDirection: 'row',
    gap: 10,
  },
});
