import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  scrollView: {
    maxHeight: 110,
  },
  container: {
    paddingLeft: 20,
    flexDirection: 'row',
    gap: 20,
  },
});
