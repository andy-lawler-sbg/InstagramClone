import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  scrollView: {
    maxHeight: 110,
    paddingBottom: 10,
  },
  container: {
    paddingLeft: 20,
    flexDirection: 'row',
    gap: 20,
  },
});
